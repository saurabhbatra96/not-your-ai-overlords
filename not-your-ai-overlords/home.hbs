{{!< default}}

{{> hero}}

<div class="wrap">
    <div class="entries">
        {{#foreach posts limit="3"}}
            {{> post-entry}}
        {{/foreach}}
    </div>

    <p class="entries__more">
        <a class="link-arrow" href="{{@site.url}}/findings/">Everything in the archive {{> "icons/arrow"}}</a>
    </p>
</div>

{{> bureau}}
